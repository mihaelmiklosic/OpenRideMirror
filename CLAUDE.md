# Claude project context

Read and follow `AGENTS.md` before editing. The short version: OpenRideMirror is a source-only Garmin-to-ESP32 cycling display, ORM protocol v1 is compatibility-sensitive, maps/builds/keys stay under ignored `.orm/`, and claims require real validation. Use `orm protocol check`, `orm test`, both ESP builds and relevant Garmin builds before reporting success.
